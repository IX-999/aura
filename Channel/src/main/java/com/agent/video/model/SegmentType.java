package com.agent.video.model;

public enum SegmentType {
    /** Narration covered by a generated still image ([B-ROLL: ...]). */
    BROLL,
    /** Narration covered by an on-screen text card ([GRAPHIC: ...]). */
    GRAPHIC
}
